-- ServerScriptService > Script (isim: RagdollDeathSystem)

local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local Debris = game:GetService("Debris")

--// AYARLAR //--
local DEAD_BODY_LIFETIME = 20 -- saniye, klon Dead klasöründe ne kadar kalacak
local SETTLE_TIMEOUT = 5 -- saniye, ragdoll'un yere düşüp sakinleşmesi için maksimum bekleme
local SETTLE_CHECK_INTERVAL = 0.1
local SETTLE_VELOCITY_THRESHOLD = 0.6 -- bu hızın altına inince "durdu" sayılır

-- Kullanıcı "Dead" klasörünü Workspace'e kendisi ekleyecek; burada sadece bekliyoruz.
local deadFolder = Workspace:WaitForChild("Dead", 30)
if not deadFolder then
	warn("[RagdollDeathSystem] Workspace içinde 'Dead' adında bir Folder bulunamadı. Script devam edecek ama klonlar konulamayacak; lütfen Workspace'e 'Dead' isimli bir Folder ekleyin.")
end

--// R15 EKLEM HARİTASI (parça ve ek isimleri zaten karakterde hazır geliyor) //--
local R15_JOINTS = {
	{"UpperTorso", "LowerTorso", "WaistRigAttachment", "WaistRigAttachment"},
	{"Head", "UpperTorso", "NeckRigAttachment", "NeckRigAttachment"},
	{"UpperTorso", "LeftUpperArm", "LeftShoulderRigAttachment", "LeftShoulderRigAttachment"},
	{"LeftUpperArm", "LeftLowerArm", "LeftElbowRigAttachment", "LeftElbowRigAttachment"},
	{"LeftLowerArm", "LeftHand", "LeftWristRigAttachment", "LeftWristRigAttachment"},
	{"UpperTorso", "RightUpperArm", "RightShoulderRigAttachment", "RightShoulderRigAttachment"},
	{"RightUpperArm", "RightLowerArm", "RightElbowRigAttachment", "RightElbowRigAttachment"},
	{"RightLowerArm", "RightHand", "RightWristRigAttachment", "RightWristRigAttachment"},
	{"LowerTorso", "LeftUpperLeg", "LeftHipRigAttachment", "LeftHipRigAttachment"},
	{"LeftUpperLeg", "LeftLowerLeg", "LeftKneeRigAttachment", "LeftKneeRigAttachment"},
	{"LeftLowerLeg", "LeftFoot", "LeftAnkleRigAttachment", "LeftAnkleRigAttachment"},
	{"LowerTorso", "RightUpperLeg", "RightHipRigAttachment", "RightHipRigAttachment"},
	{"RightUpperLeg", "RightLowerLeg", "RightKneeRigAttachment", "RightKneeRigAttachment"},
	{"RightLowerLeg", "RightFoot", "RightAnkleRigAttachment", "RightAnkleRigAttachment"},
}

--// R6 EKLEM HARİTASI: R6'da hazır Attachment yok, bu yüzden Motor6D'nin C0/C1 CFrame'lerini
--// kendimiz Attachment olarak kuruyoruz. Değerler R6 rigi için standart, doğrulanmış CFrame'lerdir.
local R6_JOINTS = {
	{
		part0 = "Torso", part1 = "Head", motorName = "Neck",
		c0 = CFrame.new(0, 1, 0, 0, -1, 0, 1, 0, 0, 0, 0, 1),
		c1 = CFrame.new(0, -0.5, 0, 0, -1, 0, 1, 0, 0, 0, 0, 1),
	},
	{
		part0 = "Torso", part1 = "Left Arm", motorName = "Left Shoulder",
		c0 = CFrame.new(-1.3, 0.75, 0, -1, 0, 0, 0, -1, 0, 0, 0, 1),
		c1 = CFrame.new(0.2, 0.75, 0, -1, 0, 0, 0, -1, 0, 0, 0, 1),
	},
	{
		part0 = "Torso", part1 = "Right Arm", motorName = "Right Shoulder",
		c0 = CFrame.new(1.3, 0.75, 0, 1, 0, 0, 0, -1, 0, 0, 0, -1),
		c1 = CFrame.new(-0.2, 0.75, 0, 1, 0, 0, 0, -1, 0, 0, 0, -1),
	},
	{
		part0 = "Torso", part1 = "Left Leg", motorName = "Left Hip",
		c0 = CFrame.new(-0.5, -1, 0, -1, 0, 0, 0, 0, 1, 0, 1, 0),
		c1 = CFrame.new(0, 1, 0, -1, 0, 0, 0, 0, 1, 0, 1, 0),
	},
	{
		part0 = "Torso", part1 = "Right Leg", motorName = "Right Hip",
		c0 = CFrame.new(0.5, -1, 0, 1, 0, 0, 0, 0, -1, 0, 1, 0),
		c1 = CFrame.new(0, 1, 0, 1, 0, 0, 0, 0, -1, 0, 1, 0),
	},
}

--// YARDIMCI FONKSİYONLAR //--

-- R15 karakterin Avatar Joint Upgrade (yeni sistem) mi yoksa klasik Motor6D mi kullandığını tespit eder.
local function usesAvatarJointUpgrade(character)
	for _, descendant in ipairs(character:GetDescendants()) do
		if descendant:IsA("AnimationConstraint") then
			return true
		end
	end
	return false
end

-- Aksesuarların (saç, şapka, vb.) ragdoll sırasında düşmemesi için Handle'larını
-- sağlam bir WeldConstraint ile ilgili vücut parçasına yeniden bağlar.
-- Roblox aksesuarları normalde bir "AccessoryWeld" adlı Weld ile bağlar; bu weld
-- Motor6D'ler devre dışı kalınca veya karakterin parçaları ragdoll'da ayrı ayrı
-- fizik cismi gibi davranmaya başlayınca kopabiliyor veya gevşeyebiliyor.
-- Burada onun yerine kalıcı bir WeldConstraint kuruyoruz ki hiçbir durumda ayrılmasın.
local function securizeAccessories(character)
	for _, accessory in ipairs(character:GetChildren()) do
		if accessory:IsA("Accessory") then
			local handle = accessory:FindFirstChild("Handle")
			if not handle then
				continue
			end

			-- Bu Handle'ın nereye takılı olması gerektiğini bul: mevcut AccessoryWeld'in
			-- Part1'i genelde doğru hedeftir (Head, veya bazı aksesuarlarda başka bir parça).
			local existingWeld = handle:FindFirstChildOfClass("Weld")
				or handle:FindFirstChildOfClass("Motor6D")

			local attachTarget = nil
			if existingWeld then
				if existingWeld.Part0 == handle then
					attachTarget = existingWeld.Part1
				elseif existingWeld.Part1 == handle then
					attachTarget = existingWeld.Part0
				end
			end

			-- Hedef bulunamazsa (nadiren olur), Head'e düşelim; o da yoksa vazgeç.
			if not attachTarget then
				attachTarget = character:FindFirstChild("Head")
			end
			if not attachTarget then
				continue
			end

			-- Zaten sağlam bir WeldConstraint kurulmuşsa tekrar oluşturma.
			local alreadySecured = false
			for _, existing in ipairs(handle:GetChildren()) do
				if existing:IsA("WeldConstraint") and existing.Name == "RagdollAccessoryWeld" then
					if (existing.Part0 == handle and existing.Part1 == attachTarget)
						or (existing.Part1 == handle and existing.Part0 == attachTarget) then
						alreadySecured = true
						break
					end
				end
			end
			if alreadySecured then
				continue
			end

			local secureWeld = Instance.new("WeldConstraint")
			secureWeld.Name = "RagdollAccessoryWeld"
			secureWeld.Part0 = handle
			secureWeld.Part1 = attachTarget
			secureWeld.Parent = handle

			-- Handle'ın kendisi de karakterin geri kalanıyla birlikte çarpışabilir olsun,
			-- ama ayrı bir cisim gibi zıplayıp uçmasın diye Anchored kalmasına gerek yok,
			-- WeldConstraint zaten pozisyonunu sabitliyor.
			handle.CanCollide = false
		end
	end
end

-- R15 - YENİ SİSTEM (AJU): Zaten var olan AnimationConstraint'leri devre dışı bırakıp
-- BallSocketConstraint'lerin fizik kontrolünü almasını sağlar.
local function ragdollR15ViaAJU(character)
	for _, descendant in ipairs(character:GetDescendants()) do
		if descendant:IsA("AnimationConstraint") then
			descendant.Enabled = false
		elseif descendant:IsA("BallSocketConstraint") then
			descendant.Enabled = true
		elseif descendant:IsA("Motor6D") then
			descendant.Enabled = false
		end
	end
end

-- R15 - ESKİ SİSTEM (klasik): Motor6D'leri yok edip yerine BallSocketConstraint kurar.
local function ragdollR15ViaMotor6D(character)
	for _, jointInfo in ipairs(R15_JOINTS) do
		local part0Name, part1Name, att0Name, att1Name = jointInfo[1], jointInfo[2], jointInfo[3], jointInfo[4]

		local part0 = character:FindFirstChild(part0Name)
		local part1 = character:FindFirstChild(part1Name)
		if not (part0 and part1) then
			continue
		end

		local motor = part1:FindFirstChildOfClass("Motor6D")
		if motor and (motor.Part0 == part0 or motor.Part1 == part0) then
			motor:Destroy()
		end

		local att0 = part0:FindFirstChild(att0Name)
		local att1 = part1:FindFirstChild(att1Name)
		if not (att0 and att1) then
			continue
		end

		local socket = Instance.new("BallSocketConstraint")
		socket.Name = "RagdollSocket_" .. part0Name .. "_" .. part1Name
		socket.Attachment0 = att0
		socket.Attachment1 = att1
		socket.LimitsEnabled = true
		socket.TwistLimitsEnabled = true
		socket.Restitution = 0
		socket.Parent = part0
	end
end

-- R6: Attachment'lar karakterde hazır gelmediği için, her Motor6D'nin C0/C1 CFrame'lerinden
-- kendimiz Attachment oluşturup BallSocketConstraint kuruyoruz.
local function ragdollR6(character)
	for _, jointInfo in ipairs(R6_JOINTS) do
		local part0 = character:FindFirstChild(jointInfo.part0)
		local part1 = character:FindFirstChild(jointInfo.part1)
		if not (part0 and part1) then
			continue
		end

		local motor = part1:FindFirstChild(jointInfo.motorName)
			or part0:FindFirstChild(jointInfo.motorName)

		if motor and motor:IsA("Motor6D") then
			motor.Enabled = false
		end

		local att0 = Instance.new("Attachment")
		att0.Name = "RagdollAttachment_" .. jointInfo.motorName .. "_0"
		att0.CFrame = jointInfo.c0
		att0.Parent = part0

		local att1 = Instance.new("Attachment")
		att1.Name = "RagdollAttachment_" .. jointInfo.motorName .. "_1"
		att1.CFrame = jointInfo.c1
		att1.Parent = part1

		local socket = Instance.new("BallSocketConstraint")
		socket.Name = "RagdollSocket_" .. jointInfo.motorName
		socket.Attachment0 = att0
		socket.Attachment1 = att1
		socket.LimitsEnabled = true
		socket.TwistLimitsEnabled = true
		socket.Restitution = 0
		socket.Parent = part0
	end
end

-- Karakteri ragdoll durumuna geçirir: RigType'a bakıp doğru yolu seçer.
local function ragdollCharacter(character, humanoid)
	humanoid.BreakJointsOnDeath = false -- Roblox'un varsayılan "parçala" davranışını engelle
	humanoid:ChangeState(Enum.HumanoidStateType.Physics)
	humanoid.PlatformStand = true
	humanoid.RequiresNeck = false -- R6'da baş kopup gitmesin

	-- Aksesuarları vücuda sağlam şekilde weld'le. Bu, eklemler kırılmadan/devre dışı kalmadan
	-- ÖNCE yapılmalı ki saç/şapka gibi parçalar o an bir "boşluk" yaşamasın.
	securizeAccessories(character)

	if humanoid.RigType == Enum.HumanoidRigType.R6 then
		ragdollR6(character)
	else
		if usesAvatarJointUpgrade(character) then
			ragdollR15ViaAJU(character)
		else
			ragdollR15ViaMotor6D(character)
		end
	end

	for _, part in ipairs(character:GetDescendants()) do
		if part:IsA("BasePart") then
			part.CanCollide = true
		end
	end
end

-- Karakterin fiziksel olarak "durduğunu" tespit eder.
local function waitUntilSettled(character, timeoutSeconds)
	local startTime = os.clock()

	while os.clock() - startTime < timeoutSeconds do
		local maxSpeed = 0

		for _, part in ipairs(character:GetDescendants()) do
			if part:IsA("BasePart") then
				local speed = part.AssemblyLinearVelocity.Magnitude
				if speed > maxSpeed then
					maxSpeed = speed
				end
			end
		end

		if maxSpeed < SETTLE_VELOCITY_THRESHOLD then
			return true
		end

		task.wait(SETTLE_CHECK_INTERVAL)
	end

	return false
end

-- Ragdoll halindeki karakteri klonlar, Dead klasörüne koyar, sabitler ve zamanlayıcıya bağlar.
local function cloneToDeadFolder(character, player)
	if not deadFolder then
		return
	end

	local clone = character:Clone()
	clone.Name = (player and player.Name or character.Name) .. "_Dead"

	-- Önce silinecekleri bir listede topluyoruz, döngü bittikten SONRA yok ediyoruz.
	-- GetDescendants() çağrıldığı andaki sabit bir liste döner; döngü içinde :Destroy() çağırırsak
	-- zincirleme yok etme yüzünden listede sırada bekleyen eleman "nil" haline gelip hataya yol açabilir.
	local toDestroy = {}

	for _, part in ipairs(clone:GetDescendants()) do
		if part:IsA("BasePart") then
			part.Anchored = true
			part.CanCollide = false
		elseif part:IsA("Constraint") or part:IsA("Motor6D") or part:IsA("Attachment") or part:IsA("Weld") then
			table.insert(toDestroy, part)
		end
	end

	for _, part in ipairs(toDestroy) do
		if part and part.Parent then
			part:Destroy()
		end
	end

	local cloneHumanoid = clone:FindFirstChildOfClass("Humanoid")
	if cloneHumanoid then
		cloneHumanoid.DisplayDistanceType = Enum.HumanoidDisplayDistanceType.None
		cloneHumanoid.Health = 0
		cloneHumanoid.HealthDisplayType = Enum.HumanoidHealthDisplayType.AlwaysOff
	end

	clone.Parent = deadFolder
	Debris:AddItem(clone, DEAD_BODY_LIFETIME)

	return clone
end

--// ÖLÜM İŞLEME AKIŞI //--

local function onCharacterDied(player, character, humanoid)
	if character:GetAttribute("DeathHandled") then
		return
	end
	character:SetAttribute("DeathHandled", true)

	ragdollCharacter(character, humanoid)

	task.spawn(function()
		waitUntilSettled(character, SETTLE_TIMEOUT)

		if not character.Parent then
			return
		end

		cloneToDeadFolder(character, player)
		character:Destroy()

		if player and player.Parent then
			player:LoadCharacter()
		end
	end)
end

local function setupHumanoidTracking(player, character)
	local humanoid = character:WaitForChild("Humanoid", 5)
	if not humanoid then
		return
	end

	character:SetAttribute("DeathHandled", false)

	humanoid.Died:Connect(function()
		onCharacterDied(player, character, humanoid)
	end)
end

--// TÜM OYUNCULARA UYGULAMA (herkes için geçerli, rig tipi ve admin ayrımı yok) //--

Players.PlayerAdded:Connect(function(player)
	player.CharacterAdded:Connect(function(character)
		setupHumanoidTracking(player, character)
	end)

	if player.Character then
		setupHumanoidTracking(player, player.Character)
	end
end)

for _, player in ipairs(Players:GetPlayers()) do
	if player.Character then
		setupHumanoidTracking(player, player.Character)
	end
	player.CharacterAdded:Connect(function(character)
		setupHumanoidTracking(player, character)
	end)
end